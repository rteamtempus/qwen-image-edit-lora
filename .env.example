# Qwen Image Edit API 테스트용 환경변수 예시
# 이 파일을 복사하여 test.env 또는 .env 로 저장한 뒤 실제 값으로 채우세요.
# 프로젝트 루트에 test.env 를 두고 사용할 수도 있습니다.

# RunPod API 키 (필수)
runpod_API_KEY=your-runpod-api-key-here

# Qwen Image Edit 엔드포인트 ID (필수)
qwen_image_edit=your-endpoint-id-here

# 테스트 시 사용할 이미지 URL (선택, test_api.py --image-url 대신 사용 가능)
# TEST_IMAGE_URL=https://example.com/your-test-image.jpg

# --- 아래는 S3/Network Volume 사용 시 참고용 ---
# url=https://s3api-<region>.runpod.io/
# region=eu-ro-1
# access_key_id=your-access-key
# bucket_name=your-bucket-name
# secret_access_key=your-secret-key
